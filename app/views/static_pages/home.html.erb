<% if signed_in? %>
	<div class="row">
		<aside class="span4">
			<section>
				<%= render 'shared/user_info' %>
			</section>
		</aside>
	</div>
<% else %>
	<div class="center hero-unit">
		<h1>Welcome to Sample App</h1>
		<h2>Home page for sample app</h2>
		<%= link_to "Sign up today!", signup_path, :class => "btn btn-large btn-primary" %>
	</div>
<% end %>
